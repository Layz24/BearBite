{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:3000",  // Make sure the port matches your server
            "webRoot": "${workspaceFolder}"  // Adjust this if your files are in a different folder
        }
    ]
}
